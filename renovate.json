{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard",
    ":semanticCommits"
  ],
  "dependencyDashboardAutoclose": true,
  "timezone": "America/Halifax",
  "schedule": ["before 6am on monday"],
  "prConcurrentLimit": 5,
  "labels": ["dependencies"],
  "flux": {
    "managerFilePatterns": [
      "clusters/**/*.yaml",
      "infrastructure/**/*.yaml",
      "apps/**/*.yaml"
    ]
  },
  "kubernetes": {
    "managerFilePatterns": [
      "infrastructure/**/*.yaml",
      "apps/**/*.yaml"
    ]
  },
  "customManagers": [
    {
      "description": "Track container image tags pinned inside HelmRelease values. The flux manager only sees chart versions, so an image pinned to work around a stale chart would otherwise never be updated.",
      "customType": "regex",
      "managerFilePatterns": [
        "infrastructure/**/*.yaml",
        "apps/**/*.yaml"
      ],
      "matchStrings": [
        "#\\s*renovate:\\s*image=(?<depName>\\S+)\\s+tag:\\s*[\"']?(?<currentValue>[^\"'\\s]+)"
      ],
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "description": "Patch updates are low risk and can merge themselves. Overridden for Cilium and local-path-provisioner below.",
      "matchUpdateTypes": ["patch"],
      "automerge": true,
      "automergeType": "pr",
      "minimumReleaseAge": "3 days"
    },
    {
      "description": "Group minor bumps of the supporting stack into one reviewable PR.",
      "matchPackageNames": ["metrics-server", "homepage", "linkding"],
      "matchUpdateTypes": ["minor"],
      "groupName": "cluster addons"
    },
    {
      "description": "local-path-provisioner owns every persistent volume in the cluster, and its chart is still 0.0.x, so even a z-bump can change provisioning behaviour. Never automerged. Listed after the patch rule so it overrides it.",
      "matchPackageNames": ["local-path-provisioner"],
      "groupName": "storage",
      "automerge": false,
      "minimumReleaseAge": "7 days",
      "addLabels": ["storage", "review-carefully"]
    },
    {
      "description": "Cilium is the CNI and the kube-proxy replacement. A bad upgrade takes cluster networking with it, so it is never automerged and always lands on its own. Listed last so it overrides the patch-automerge rule above.",
      "matchPackageNames": ["cilium"],
      "groupName": "cilium",
      "automerge": false,
      "minimumReleaseAge": "7 days",
      "addLabels": ["cni", "review-carefully"]
    },
    {
      "description": "Flux's own controllers are upgraded with `flux bootstrap`, which rewrites gotk-components.yaml. Renovate must not edit that file or the two will fight.",
      "matchFileNames": ["clusters/**/flux-system/**"],
      "enabled": false
    }
  ]
}
