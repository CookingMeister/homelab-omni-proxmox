---
apiVersion: notification.toolkit.fluxcd.io/v1beta3
kind: Provider
metadata:
  name: ntfy
  namespace: flux-system
spec:
  type: generic
  # address and headers both come from the encrypted secret.
  secretRef:
    name: ntfy-provider
